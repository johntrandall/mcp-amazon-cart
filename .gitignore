# Dependencies
node_modules/
# package-lock.json is COMMITTED deliberately (2026-09-24). Upstream
# (madebydia) gitignores it, and that inheritance meant every Portainer
# git-backed build fell through the Dockerfile's `npm ci || npm install`
# fallback to `npm install` — resolving the whole dependency tree fresh on
# each build. Digest-pinning the base image did not help while npm floated.
# This is a deployed application, not a library: the lockfile belongs in git.
yarn.lock

# Build output
dist/
build/
*.tsbuildinfo

# Environment variables (SECURITY SENSITIVE)
.env
.env.local
.env.*.local
.env.production

# Browser user data (SECURITY SENSITIVE - contains session/login info)
user-data/
*.crx
*.pem

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov
.nyc_output

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
*.swm
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace

# Temporary files
*.tmp
*.temp
.cache/
.temp/

# Test files (if added later)
.jest/
test-results/

# macOS
.AppleDouble
.LSOverride

# Windows
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Linux
.fuse_hidden*
.directory
.Trash-*
.nfs*
